# NigehbaanDastak Frontend

This frontend stays intentionally thin during the scheduling phase. It currently
supports:

- appointment list with simple status updates
- create appointment form
- create visit and visit detail pages
- existing patient list/detail/create-edit support
- demo role switching between `admin` and `doctor`
